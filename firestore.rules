rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ARC full-stack server runs server-side API proxy with complete RBAC and PIN authentication.
    // Database access is mediated through server-side endpoints.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
